# ADR-0018 — Friendly Names, Dashboard Header & Persisted Agent Prompts

Status: Accepted

## Context

La consola y los dashboards (ADR-0011, ADR-0013, ADR-0017) mostraban los canales
por su id canónico (`discord:1221321…`), poco legible. Además:

1. Las **instrucciones extra** por agente (ADR-0017) sólo vivían en el `textarea`
   del navegador: al recargar o cambiar de pestaña se perdían y **no se
   persistían**. El pipeline las tomaba del DOM en el momento de ejecutar.
2. En la pestaña **Agentes** no se veía el **prompt base** que las instrucciones
   aumentan.
3. El **grafo** de trazabilidad crecía sin límite: con pocos nodos, éstos se
   veían gigantes (`max-width:none`, `min-width:max-content`).

## Decision

### Nombres legibles (canal / foro / repo)

- Nuevo `kaos.plugins.connectors.discord_rest`: helpers REST de sólo lectura
  (`fetch_channel`, `fetch_guild`, `count_admin_roles`) sin dependencia de
  discord.py. Devuelven datos normalizados (nombre, topic, tipo, contadores).
- Nuevo `kaos.plugins.dashboard.directory`: resuelve `workspace → nombre`
  **best-effort** y memoizado por proceso. Sin token (o ante cualquier error de
  red/HTTP) cae al id crudo; GitHub se muestra como `owner/repo`.
- `GET /api/subscriptions` agrega `name`; `GET /api/workspaces` agrega `labels`.
  Suscripciones, vista previa y la lista de dashboards muestran el nombre y dejan
  el id como dato secundario. Las secciones del dashboard titulan con el nombre.

### Encabezado del dashboard con indicadores

- Cuando el dashboard mira **un** workspace de Discord, `render_dashboard` recibe
  un `header` con detalles del canal (nombre, topic, tipo) y del guild (nombre,
  miembros aproximados, en línea, boosts y cantidad de **roles con permiso de
  administrador** — proxy barato que no requiere el intent privilegiado de
  miembros). Todo es best-effort: sin token, se muestra el encabezado simple.

### Prompt base + instrucciones persistidas por agente

- `RuntimeConfig` gana `agent_instructions: dict[str, str]` (estado durable, no
  secreto — como el provider/modelo). Se persiste en `PostgresConfigStore` con una
  columna `jsonb` y migración `ADD COLUMN IF NOT EXISTS` (retrocompatible); en
  memoria funciona sin cambios.
- `GET /api/agents` expone `base_prompt` (importado desde los plugins de agentes)
  e `instructions` (lo persistido). `PUT /api/agents/{id}/instructions` guarda con
  **read-modify-write**, preservando provider/modelo y las instrucciones de otros
  agentes. Guardar vacío limpia el override.
- Guardar el provider preserva `agent_instructions`. Las rutas de preview/run
  usan las instrucciones del request y, si vienen vacías, **caen a las
  persistidas** — así una preferencia guardada aplica también a corridas
  headless/agendadas.
- La UI muestra el prompt base (colapsable), precarga el `textarea` con lo
  guardado y agrega **Guardar instrucciones**.

### Grafo responsivo

- CSS del grafo: `max-width:100%` en el SVG (en vez de `none`), fuentes y trazos
  más finos (`stroke-width:1px`), y `flowchart.useMaxWidth:true` +
  `themeVariables.fontSize` en la init de Mermaid, para que pocos nodos escalen al
  contenedor en lugar de agrandarse.

## Consequences

- La operación deja de leer ids crudos: nombres de canal/foro/repo en toda la
  consola y el dashboard, con un encabezado rico cuando hay token.
- Las instrucciones por agente ahora **sobreviven** recargas y reinicios, y se
  aplican de forma consistente (UI, preview, run y corridas headless).
- El Core sigue agnóstico: los nombres se resuelven en la capa de plugins
  (dashboard/connectors); sin token todo degrada al id sin romperse.
- Nuevo costo: llamadas REST a Discord al listar/renderizar (mitigadas con
  memoización por proceso y fallback silencioso).

